// ShortyPro dashboard config (safe to commit; no secrets).
// Wire these to your real backend once endpoints are live.
window.SHORTYPRO = {
  apiBase: "/api",
  uploadEndpoint: "/api/uploads",
  shortsEndpoint: "/api/shorts",
  scheduleEndpoint: "/api/schedule",
  accountsEndpoint: "/api/accounts",
  billingPortalUrl: "/dashboard/billing.html", // replace with Stripe Customer Portal link when ready
};