# Backend base URL (Render)
NEXT_PUBLIC_API_BASE=https://shortypro-backend.onrender.com

# If you want the buttons to go straight to Stripe hosted links, set these.
# Otherwise frontend will call the backend endpoints to create sessions.
NEXT_PUBLIC_STRIPE_CHECKOUT_ENDPOINT=/stripe/create-checkout-session

# Optional
NEXT_PUBLIC_SITE_NAME=Shorty Pro
