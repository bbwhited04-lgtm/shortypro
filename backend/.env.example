APP_NAME=ShortyPro
# Render Postgres supplies this automatically in production
DATABASE_URL=postgresql://user:pass@host:5432/dbname

# Admin bootstrap user (created if no users exist)
ADMIN_EMAIL=billy@deadapp.info
ADMIN_PASSWORD=change-me-now

# JWT
JWT_SECRET=replace-with-a-long-random-string
JWT_ISSUER=shortypro-backend
JWT_AUDIENCE=shortypro-frontend
ACCESS_TOKEN_TTL_MINUTES=10080

# CORS (comma-separated)
CORS_ORIGINS=http://localhost:3000,https://shortypro.com
